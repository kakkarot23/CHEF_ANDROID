import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFFFF5722); // Warm Deep Coral / Flame Orange
  static const Color primaryDark = Color(0xFFE64A19);
  static const Color primaryLight = Color(0xFFFFCCBC);
  
  // Secondary / Accent
  static const Color accentGold = Color(0xFFFFB300); // Warm Gold / Star Rating
  static const Color accentGreen = Color(0xFF4CAF50); // Fresh Mint / Vegan tag
  static const Color accentTeal = Color(0xFF00897B); // Specialization badge

  // Backgrounds
  static const Color background = Color(0xFFF9F9FB); // Off-white / Cream background
  static const Color cardBackground = Colors.white;
  static const Color darkBackground = Color(0xFF121218);
  static const Color darkCardBackground = Color(0xFF1E1E28);

  // Text & Neutral
  static const Color textPrimary = Color(0xFF1F2024);
  static const Color textSecondary = Color(0xFF71747D);
  static const Color textMuted = Color(0xFFA0A3BD);
  static const Color border = Color(0xFFEAEBED);

  // Status
  static const Color verifiedBlue = Color(0xFF1DA1F2);
  static const Color heartRed = Color(0xFFE53935);
}
